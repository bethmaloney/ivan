/* Substituted by esbuild at build time (build.mjs), replacing what CMake's
   configure_file used to put into harness-pre.js.in. */

declare const IVAN_BUILD_ID: string;
declare const IVAN_CRASH_ENDPOINT: string;
