/* Substituted by esbuild at build time (build.mjs). Both used to come from
   CMake's configure_file into harness-pre.js.in; that file and the CMake
   plumbing behind it are gone, and build.mjs is the only source of either. */

declare const IVAN_BUILD_ID: string;
declare const IVAN_CRASH_ENDPOINT: string;
