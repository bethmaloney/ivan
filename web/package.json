{
  "name": "ivan-web",
  "version": "0.0.0",
  "private": true,
  "description": "The browser frontend for IVAN: everything the page owns rather than the wasm module (HARNESS.md §9)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "lint": "oxlint",
    "test": "node --test 'test/**/*.test.ts'",
    "check": "npm run typecheck && npm run lint && npm test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/emscripten": "^1.41.5",
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.2",
    "oxlint": "^1.78.0",
    "typescript": "^7.0.2"
  },
  "//": "@types/node tracks the runtime, not npm: 26.x is current but this is pinned to 22.x because Node 22 is what CI installs and what strips the types. Newer would describe APIs the runtime does not have."
}
